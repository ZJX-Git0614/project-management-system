## 2026-05-29 项目状态

### 已完成的核心功能

- 认证权限体系：真实 JWT 登录/登出、树形权限配置、账号管理
- 必换件/选换件/结构件：清单维护、推送管理、采购执行闭环
- 待办中心：待办数量红圈提示
- 操作历史：全流程留痕，中文格式化
- Docker 部署支持：Dockerfile + docker-compose.yml + Windows 部署指南
- GJB438C 标准总结文档

### 数据库

- SQLite (Prisma ORM)
- 10 个数据模型
- 种子数据仅包含基础角色、管理员账号和默认权限树，不写入演示项目或事项
- JWT 密钥、数据库路径可通过环境变量配置

### API 端点

36+ 个 REST 端点，覆盖所有 CRUD 操作和业务逻辑（推送、采购、待办、操作历史等）。

### 待办

- 文件上传的 fileDataUrl 仍存为 base64，后续可改为文件存储
- 仪表盘页面（/api/dashboard 已实现，前端页面未完成）
